#include <string>
#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

class AddMul
{
    int add(vector<int> numbers);
    int multiply(vector<int> numbers);
    void test();
};